<article class="tease">
	<h2 class="tease__title"><a href="{{ post.link }}">{{ post.title }}</a></h2>
	<p class="tease__preview">{{post.preview}}</p>
	{% if post.get_thumbnail %}
		<img src="{{ post.thumbnail.src }}" class="tease__image">
	{% endif %}
</article>